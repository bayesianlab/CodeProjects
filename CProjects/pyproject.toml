[project]
name = "cprojects"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Dillon", email = "dillonflann@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "pandas>=3.0.1",
    "seaborn>=0.13.2",
]

[build-system]
requires = ["uv_build>=0.10.8,<0.11.0"]
build-backend = "uv_build"
